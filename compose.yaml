services:

  frontend:
    build:
      context: ./frontend
    ports:
    - "8000:8000"
  db:
    build:
      context: ./db
    ports:
    - "8001:8001"
    volumes:
    - db_data:/var/lib/sqlite

volumes:
  db_data: